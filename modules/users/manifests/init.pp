class users {
  @user { 'hunner':  groups => ['dev','ops'],}
  @user { 'toor':    groups => ['dev','ops'],}
  @user { 'elvis':   groups => ['dev'],}
  @user { 'bobm':    groups => ['dev'],}
  @user { 'jmarley': groups => ['ops'],}
}
